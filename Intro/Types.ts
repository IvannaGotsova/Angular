let string: string = "Ivan";
let number: number = 30;
let isTrue: boolean = true;

let example: string = `The string is ${string}, the numbers is ${number}, the bool is ${isTrue}.`;

console.log(example)